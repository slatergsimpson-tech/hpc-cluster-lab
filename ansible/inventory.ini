# Cluster inventory — the single source of truth for node names and addresses.
# All addresses are on the VirtualBox host-only network (192.168.56.0/24),
# the private "cluster network" shared by the VMs and the Windows host.

[head]
head01 ansible_host=192.168.56.10

[compute]
node01 ansible_host=192.168.56.11
node02 ansible_host=192.168.56.12

[cluster:children]
head
compute

[cluster:vars]
ansible_user=slater
ansible_ssh_private_key_file=~/.ssh/hpc-lab
